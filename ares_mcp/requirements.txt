# ARES MCP Integration Module - Requirements
# Install with: pip install -r requirements.txt

# HTTP Client for async communication with Hexstrike server
httpx>=0.27.0

# Redis client for result caching
redis>=5.0.0

# Async Redis support
# Note: redis package now includes async support by default

# Logging (if not already installed)
loguru>=0.7.0

# Already included in ARES core but listed for reference:
# sqlalchemy>=2.0.0  # Database ORM
# asyncpg>=0.29.0    # PostgreSQL async driver
# pydantic>=2.0.0    # Configuration management
